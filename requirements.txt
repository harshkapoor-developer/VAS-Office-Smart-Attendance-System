# Smart Attendance System - Phase 0 dependencies
# Install order matters: numpy/cmake/dlib BEFORE face_recognition.
# dlib has no prebuilt Windows wheels on PyPI -> it compiles from source.
# You MUST have "Desktop development with C++" (Visual Studio Build Tools)
# and CMake installed and on PATH before running `pip install -r requirements.txt`.

cmake>=3.28.0
numpy>=1.26.0,<2.0.0
# Pinned below 20.0.0: dlib 20.0.0/20.0.1 changed compute_face_descriptor()'s
# call signature, which face_recognition 1.3.0 (unmaintained since ~2021)
# calls the old way - that combination crashes every face encoding attempt.
# 19.24.x is the last line tested against face_recognition 1.3.0.
dlib>=19.24.0,<20.0.0
face_recognition>=1.3.0
face-recognition-models>=0.3.0
opencv-python>=4.9.0
customtkinter>=5.2.2
pillow>=10.2.0
pandas>=2.2.0
openpyxl>=3.1.2
matplotlib>=3.8.0
reportlab>=4.0.0
pyserial>=3.5
pytest>=8.0.0
pytest-cov>=5.0.0

# Raspberry Pi only - installed via apt (python3-picamera2), not pip.
# Listed here for documentation; do not `pip install picamera2` on
# Windows/regular Linux, it will fail since it depends on libcamera
# system libraries that don't exist off-Pi. See PI_DEPLOYMENT.md.
# picamera2
